{
  "name": "playwright-ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "debugLastFailedTest": "npx playwright test --last-failed --debug",
    "tests": "npx bddgen && npx playwright test --project chromium --headed",
    "dummyTests": "npx bddgen && npx playwright test --project chromium --grep \"@dummyTag\"",
    "desktopTests": "npx bddgen && npx playwright test --project chromium --project firefox --project webkit",
    "mobileTests": "npx bddgen && npx playwright test --project \"Mobile Chrome\" --project \"Mobile Safari\"",
    "test": "cucumber-js"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.3",
    "@types/node": "^22.0.2",
    "playwright-bdd": "^7.5.0"
  }
}
